{
  "name": "agent-code-validation",
  "version": "0.1.0",
  "private": true,
  "description": "Root workspace for the agent-code-validation Claude Code plugin.",
  "workspaces": [
    "mcp-server"
  ],
  "scripts": {
    "build": "npm -w mcp-server run build",
    "test": "npm -w mcp-server run test && python tests/cheating_corpus/harness.py",
    "lint": "npm -w mcp-server run lint || true",
    "docker:build": "npm -w mcp-server run build && npm -w mcp-server run docker:build"
  },
  "engines": {
    "node": ">=20"
  }
}
